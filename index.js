var katzDeli = []

function takeANumber(katzDeli, name) {
  for (let i = 3; i < 4; i++) {
    array.push(`Welcome name, you are number 1 in line`)}
    return 
}
var otherDeli = ["Steven", "Blake", "Avi"]

function takeANumber(otherDeli, name) {
  var newDeli = [...number, name]
  return newDeli}
 
  
  